/**
 * Feature flags — set to `true` to enable, `false` to hide entirely.
 * Change FEATURE_DICTATION to true once the voice-dictation backend is production-ready.
 */
export const FEATURE_DICTATION = false;
